# Application environment
APP_ENV=development
PORT=8080

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=username
DB_PASS=password
DB_NAME=testdb
DB_SSLMODE=disable

# JWT secret (for auth if needed)
JWT_SECRET=supersecret